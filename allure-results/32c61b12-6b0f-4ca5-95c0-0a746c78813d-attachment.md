# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: learn\screenshots\screenshot.spec.ts >> Capture screenshot
- Location: tests\learn\screenshots\screenshot.spec.ts:3:5

# Error details

```
Error: expect(locator).toHaveText(expected) failed

Locator: locator('h1')
Timeout: 5000ms
- Expected  - 1
+ Received  + 6

- Non-Existing Title
+
+
+ Wikipedia
+
+ The Free Encyclopedia
+

Call log:
  - Expect "toHaveText" with timeout 5000ms
  - waiting for locator('h1')
    13 × locator resolved to <h1 class="central-textlogo-wrapper">…</h1>
       - unexpected value "

Wikipedia

The Free Encyclopedia
"

```

```yaml
- heading "Wikipedia The Free Encyclopedia" [level=1]:
  - text: Wikipedia
  - strong: The Free Encyclopedia
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test('Capture screenshot', async ({ page }, testInfo) => {
  4  |     await page.goto('https://www.wikipedia.org/');
  5  |     //Element level SS
  6  |     const searchPage = await page.locator('input[name="search"]');
  7  |     await searchPage.screenshot({path: 'snapshots/search_box.png'});
  8  |     //Page level SS
  9  |     await page.screenshot({ path: 'snapshots/full_page.png', fullPage: true });
  10 | 
  11 |     // Capture screenshot and attach it to the report
  12 |     const screenshot = await page.screenshot();
  13 |     testInfo.attach('screenshot', {body: screenshot, contentType: 'image/png' });
  14 | 
  15 |     try {
  16 |     // Intentional failure
> 17 |     await expect(page.locator('h1')).toHaveText('Non-Existing Title');
     |                                      ^ Error: expect(locator).toHaveText(expected) failed
  18 |     }   catch (error) {
  19 |     await page.screenshot({ path: 'snapshots/error_screenshot.png' });
  20 |     throw error; // Rethrow the error after capturing the screenshot
  21 |     }
  22 | });
```